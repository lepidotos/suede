; -*- scheme -*-

(define-object CappletWidget (GtkPlug))

(define-func capplet_widget_new
  GtkWidget
  ())

; capplet_gtk_main
; gnome_capplet_init

(define-func capplet_widget_state_changed
  none
  ((CappletWidget cap)
   (bool undoable)))