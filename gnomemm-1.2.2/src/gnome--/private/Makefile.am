
new_p_h = $(patsubst %.gen_h,%_p.h,$(GNOME_GEN_H_FILES))
includedir = $(prefix)/include/gnome--/private
include_HEADERS = $(new_p_h)
#BUILT_SOURCES = $(new_p_h)
DISTFILES = $(DIST_COMMON) $(TEXINFOS) $(EXTRA_DIST)
