To build with Microsoft's tools, say nmake -f makefile.msc.

To build with gcc (read GLib's README.win32 for instructions on
compiler version and configuration), build with make -f makefile.cygwin.

--tml@iki.fi
