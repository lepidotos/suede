- add callbacks to properties dialog so properties can be applied and
  saved
- separate ProcProcData mainpulation, so it can be used in "details"
- tooltips for proclist column titles
- toolbar
  - kill, term
- process tree
- save state
  - save clist geometry
    this could be done global (gnome_clist widget, like gnome_entry)
