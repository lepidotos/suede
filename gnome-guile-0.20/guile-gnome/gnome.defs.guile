;; -*- scheme -*-

(add-options gnome_client_request_interaction_interp (protection client))

(options
 (includes "#include <gnome.h>")
 (init-func "gnome_init_guile_glue")
 (extra-init-code "gnome_init_guile_support ();"))

 
