#
# mount-type-stoplist
#
# This file contains a list of filesystem types, one per line.
# Medusa will refuse to index mounted filesystems that are of
# the listed types. This is mostly useful to preventing 
# medusa from indexing network shares.
#
nfs
afs
shm
autofs
smbfs
